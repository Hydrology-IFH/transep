name: transep

channels:
    - conda-forge

dependencies:
    - python=3.8
    - pip
    - click
    - numpy
    - scipy
    - pandas
    - matplotlib
    - seaborn
    - tqdm
    - pip:
        - diag-eff
